import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'premium-test-task',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
